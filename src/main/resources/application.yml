spring:
  datasource:
    url: jdbc:mysql://localhost:13306/library?useSSL=false&allowPublicKeyRetrieval=true
    username: library_user
    password: library_password
  jpa:
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
